#!/usr/bin/env sh
echo "🚀 Running pre-commit checks..."

# Проверяем наличие lint-staged
if command -v npx >/dev/null 2>&1; then
    npx lint-staged
else
    echo "❌ npx not available"
    exit 1
fi